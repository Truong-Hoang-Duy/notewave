{
  "name": "notewave-workspace",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node run-dev.js",
    "start": "node run-dev.js",
    "client": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "test:server": "cd server && .venv/Scripts/python -m pytest"
  }
}
